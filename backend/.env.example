# ========================================
# 🔒 夢ログ バックエンド環境変数テンプレート
# ========================================
# 
# このファイルをコピーして backend/.env を作成してください
# cp backend/.env.example backend/.env
# 
# ⚠️ 重要: 本番環境では絶対に機密情報をコミットしないでください

# ========================================
# 🗄️ データベース設定
# ========================================
# PostgreSQLのパスワード（16文字以上の複雑なパスワードを設定）
POSTGRES_PASSWORD=your_secure_database_password_here

# Render/Supabaseなどの外部PostgreSQLを使う場合はこちらを優先
# DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DB_NAME

# ========================================
# 🔑 Rails セキュリティキー
# ========================================
# Railsマスターキー（config/master.keyの内容をコピー）
RAILS_MASTER_KEY=your_rails_master_key_here

# Railsシークレットキー（`rails secret`で生成）
SECRET_KEY_BASE=your_rails_secret_key_base_here

# ========================================
# 🤖 外部API設定
# ========================================
# OpenAI APIキー（https://platform.openai.com/api-keysで取得）
OPENAI_API_KEY=your_openai_api_key_here

# 夢分析・月次サマリーに使うチャットモデル（省略時: gpt-4o-mini）
# 検証時: gpt-5-mini / gpt-5-nano に変更して比較できます
OPENAI_CHAT_MODEL=gpt-4o-mini

# ========================================
# 🎫 JWT認証設定
# ========================================
# JWT秘密鍵（`openssl rand -hex 64`で生成）
JWT_SECRET_KEY=your_jwt_secret_key_here

# JWTトークンの有効期限（分単位: 15 = 15分）
JWT_EXPIRATION_MINUTES=15

# ========================================
# 💳 Stripe決済設定
# ========================================
# Stripe公開可能キー（フロントエンド用、pk_test_ or pk_live_）
STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here

# Stripeシークレットキー（バックエンド用、sk_test_ or sk_live_）
STRIPE_SECRET_KEY=your_stripe_secret_key_here

# Stripe Webhook署名シークレット
# ローカル: stripe listen --print-secret で取得（ローカル検証用）
# 本番: Stripeダッシュボード → Developers → Webhooks で別途登録・発行
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret_here

# フロントエンドURL（Stripe決済後のリダイレクト先）
# ローカル: http://localhost:3000
# 本番: https://your-app.vercel.app
FRONTEND_URL=http://localhost:3000

# Stripe Subscription の Price ID（月額500円の recurring price）
STRIPE_PREMIUM_PRICE_ID=price_your_premium_subscription_price_id_here

# ========================================
# 🔍 監視・エラートラッキング設定
# ========================================
# Sentry DSN（https://sentry.ioで取得）
SENTRY_DSN=your_sentry_dsn_here

# ========================================
# 📝 環境変数設定手順
# ========================================
# 
# 1. このファイルをコピー:
#    cp backend/.env.example backend/.env
# 
# 2. 各値を実際の値に置換:
#    - POSTGRES_PASSWORD: 強力なパスワード
#    - RAILS_MASTER_KEY: config/master.keyの内容
#    - SECRET_KEY_BASE: rails secret で生成
#    - OPENAI_API_KEY: OpenAIダッシュボードから取得
#    - JWT_SECRET_KEY: openssl rand -hex 64 で生成
# 
# 3. ファイル権限を制限:
#    chmod 600 backend/.env
# 
# 4. 機密情報の確認:
#    grep -v '^#' backend/.env | grep -v '^$'

# ========================================
# 📊 監視・ログ設定
# ========================================
# アプリケーションバージョン（監視用）
APP_VERSION=1.0.0

# 構造化ログ有効化（本番環境推奨）
STRUCTURED_LOGGING=false

# 詳細デバッグログ有効化（開発環境のみ）
DEBUG_TOOLS=false
VERBOSE_LOGGING=false

# パフォーマンス監視設定
PERFORMANCE_MONITORING=false
SLOW_QUERY_THRESHOLD_MS=100

# 外部監視サービス設定（任意）
NEWRELIC_LICENSE_KEY=your_newrelic_license_key_here
DATADOG_API_KEY=your_datadog_api_key_here
